#include <stdio.h>

int main() {
    int N, contador = 0; 

    scanf("%d", &N);

    while(contador <= N) {
        printf("%d\n", contador);
        // contador = contador + 1;
    }
    
    return 0;
}
